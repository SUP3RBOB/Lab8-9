#include "another_classifier.h"

using namespace std;

// Unfinished method that prints out its own name
void AnotherClassifier::train(string filePath) {
	cout << "AnotherClassifier::train" << endl;
}

// Unfinished method that prints out its own name
void AnotherClassifier::test(string filePath, int& outErrors) {
	cout << "AnotherClassifier::test" << endl;
}

// Unfinished method that prints out its own name
void AnotherClassifier::predict(string filePath) {
	cout << "AnotherClassifier::predict" << endl;
}
